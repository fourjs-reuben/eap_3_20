This example shows a typical home screen.  THis is the first screen a user
will typically see, and allows them to get help, about, info type screens 
before drilling down into the app.

The key aspect is to have an IMAGE that takes up the whole of the screen, that
is clickable to allow the user to continue whilst there are a few extra 
clickable entries around the side for use by help, about type screens