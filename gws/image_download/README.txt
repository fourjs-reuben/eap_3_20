This example downloads images to the Web Service client.

With test program, click Clear to clear the data and any image files.
Then click Load which will return a list of image filenames
Then click Get which will get the image files.

The example program has two image files shipped with the server program,
smiley.png and ssmiley.png.   You should see these appear in the client
folder